{
  "name": "leafybus-demo",
  "version": "1.0.0",
  "private": true,
  "description": "LeafyBus PNR query API for YatriYum integration",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --watch --env-file=.env --experimental-strip-types src/server.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "typescript": "^5.9.3"
  }
}
